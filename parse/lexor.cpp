#include"main.h"
/*
int lexor()
{

    string word;
    word.clear();

    /*myfile.open("outputSQL.txt", ios::app);
    cout << "Entering into file creation Zone!!!" << endl;
    */
/*
    if(ip_file.is_open())
    {
        while(ip_file >> word)
        {
            cout<<word;
        }
    }

    //myfile.close();
    ip_file.close();
    return 0;
}

*/
